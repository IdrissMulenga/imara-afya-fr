# Copy to .env and fill in. .env is gitignored.
#
# Point the app at a running backend. Leave blank in development and the app
# resolves your machine over LAN from the Metro host automatically.
EXPO_PUBLIC_GRAPHQL_URL=
